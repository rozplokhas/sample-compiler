def pow(x, n) {
  i := 0;
  res := 1;
  while (i < n) {
    res := res * x;
    i := i + 1
  };
  return res
};

def fib(n) {
  i  := 0;
  a0 := 0;
  a1 := 1;
  while (i < n) {
    a1 := a1 + a0;
    a0 := a1 - a0;
    i := i + 1
  };
  return a0
};

write(pow(2, 10));
write(fib(pow(2, 4)))
